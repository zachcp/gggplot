# Build / deps
dist/
node_modules/
.vite/
.playwright-mcp/
__pycache__/
*.py[cod]

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Debug screenshots at the repo root (agent/manual capture artifacts)
/*.png

# Apple system fonts must not be committed (no redistribution license);
# the site ships only its OFL-licensed faces.
apps/site/public/fonts/SFNS*.ttf
.beads.gate.lock

# Per-gate browser build outputs (gggplot-8au); dist/ is the real artifact.
apps/site/dist-*/

# Local pre-recovery beads snapshot (RECOVERY-1.2.1); never commit.
.beads.backup-*/
